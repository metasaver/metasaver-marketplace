// ESLint flat config - simple re-export pattern
// Configuration complexity lives in @metasaver/core-eslint-config
// Replace {CONFIG_TYPE} with: base | node | vite-web | react-library

export { default } from "@metasaver/core-eslint-config/{CONFIG_TYPE}";
