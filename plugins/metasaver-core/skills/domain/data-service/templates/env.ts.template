export const env = {
  NODE_ENV: process.env.NODE_ENV || "development",
  PORT: parseInt(process.env.{PROJECT_UPPER}_SERVICE_PORT || "3000", 10),
  JWT_SECRET: process.env.{PROJECT_UPPER}_JWT_SECRET || "dev-secret",
  LOG_LEVEL: process.env.LOG_LEVEL || "info",
} as const;
