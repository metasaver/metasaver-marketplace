{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/ms-reminder-hook.js",
            "timeout": 5000
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh",
            "timeout": 5000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-dangerous.sh",
            "timeout": 5000
          }
        ],
        "_blocked_commands": [
          "=== GIT OPERATIONS ===",
          "git add",
          "git commit",
          "git push",
          "git pull",
          "git rebase",
          "git reset",
          "git merge",
          "git cherry-pick",
          "git revert",
          "git checkout",
          "git switch",
          "git restore",
          "git stash",
          "git am",
          "git apply",
          "=== MULTI-MONO ENFORCEMENT ===",
          "rm -rf node_modules* → USE: pnpm cb",
          "rm pnpm-lock.yaml → USE: pnpm cb",
          "pnpm install → USE: pnpm cb",
          "pnpm i → USE: pnpm cb",
          "pnpm add <package> → ALLOWED (proper way to add deps)",
          "npm publish → USE: pnpm publish or pnpm bp",
          "npm unpublish → USE: pnpm publish",
          "npm dist-tag → USE: pnpm publish",
          "npm version → USE: pnpm publish",
          "=== DESTRUCTIVE ===",
          "rm -rf /",
          "DROP DATABASE"
        ],
        "_note": "BROKEN: PreToolUse hooks don't fire (GitHub issue #6305 - STILL OPEN Jan 2026). Using .claude/hooks/pre-dangerous.sh workaround in each repo instead. Claude is BANNED from ALL git operations except read-only (status, log, diff, show, branch -l, fetch, reflog). MULTI-MONO: Must use root scripts for clean/install/publish."
      },
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-read-protect.sh",
            "timeout": 5000
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-env-protect.sh",
            "timeout": 5000
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/post-format.sh",
            "timeout": 10000
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-compact.sh",
            "timeout": 5000
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo; echo \"📊 Session End Summary:\"; echo \"$(date)\"; echo; echo \"Modified files:\"; git status --short 2>/dev/null | head -10 || echo \"No git changes\"; echo; echo \"✅ Session complete\"'"
          }
        ]
      }
    ]
  }
}
